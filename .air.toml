root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/hivy ./cmd/server"
  bin = "./tmp/hivy"
  full_bin = "LOG_LEVEL=debug LOG_FORMAT=text ./tmp/hivy"
  include_ext = ["go", "json"]
  exclude_dir = ["tmp", "bin", "node_modules", "apps", "packages", "docs", "scripts", "docker", "examples", "marketing", "patches", "strix_runs", "certs"]
  exclude_regex = ["_test\\.go$"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
