name: sandbox-cloud-agents-ci

on:
  push:
    branches: [main]
    paths:
      - "sandboxes/cloud-agents/**"
      - ".github/workflows/sandbox-cloud-agents-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "sandboxes/cloud-agents/**"
      - ".github/workflows/sandbox-cloud-agents-ci.yml"

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: check formatting
        working-directory: sandboxes/cloud-agents
        run: cargo fmt --all -- --check

  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: sandbox-cloud-agents-build
          workspaces: sandboxes/cloud-agents -> target
      - name: build workspace
        working-directory: sandboxes/cloud-agents
        run: cargo build --workspace --locked

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          key: sandbox-cloud-agents-clippy
          workspaces: sandboxes/cloud-agents -> target
      - name: run clippy
        working-directory: sandboxes/cloud-agents
        run: cargo clippy --workspace --locked -- -D warnings

  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: sandbox-cloud-agents-test
          workspaces: sandboxes/cloud-agents -> target
      - name: run tests
        working-directory: sandboxes/cloud-agents
        run: cargo test --workspace --locked --exclude storage-e2e
